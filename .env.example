# ─── Xray Balancer Middleware ─── #
# Скопируй в .env и заполни свои значения:
#   cp .env.example .env && nano .env

# URL панели Remnawave (обязательно)
REMNAWAVE_URL=https://panel.example.com

# Внутренний Docker-адрес subscription page (обязательно)
SUB_PAGE_URL=http://subscription-page:3010

# Путь для runtime-персистентных изменений (группы/quarantine) внутри контейнера
# Обычно задаётся в docker-compose, здесь оставлено для явности:
# CONFIG_RUNTIME_PATH=/app/runtime/config.runtime.json

# Публичный домен подписки (обязательно)
SUB_DOMAIN=sub.example.com

# API токен панели (для auto_groups и node_stats)
API_TOKEN=

# Admin API token for /admin/* endpoints. Leave empty to disable admin API.
ADMIN_TOKEN=

# Cookie для панели за nginx (egam.es). Оставь пустым если не нужен
PANEL_AUTH_COOKIE=

# Доверять X-Forwarded-For для IP-лимитов (включай только за доверенным reverse proxy)
TRUST_X_FORWARDED_FOR=false
# Явные CIDR доверенных reverse proxy. Для Docker добавь CIDR сети из docker network inspect.
TRUSTED_PROXY_CIDRS=127.0.0.1/32,::1/128

# Лимиты для /admin/* (защита от brute-force/abuse)
ADMIN_RATE_LIMIT_PER_MINUTE=60
ADMIN_RATE_LIMIT_BURST_10S=20

# Protection and bounded-resource defaults
PROTECTION_ENABLED=false
PROTECTION_FAILURES=2
PROTECTION_RELEASE_SUCCESSES=3
PROTECTION_ISOLATION_TTL_SEC=300
PROTECTION_LATENCY_THRESHOLD_MS=1500
PROTECTION_MIN_AVAILABLE_NODES=1
EMERGENCY_FALLBACK_ENABLED=true
EMERGENCY_FALLBACK_MAX_NODES=1
PROBE_SAMPLING=1
IP_LIMITER_MAX_ENTRIES=10000
ADMIN_LIMITER_MAX_ENTRIES=2000
MAX_UPSTREAM_CONCURRENCY=32
NEGATIVE_CACHE_TTL_SEC=30
CACHE_MAX_BYTES=134217728
CACHE_MAX_ITEM_BYTES=10485760
